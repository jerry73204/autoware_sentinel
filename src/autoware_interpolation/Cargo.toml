[package]
name = "autoware_interpolation"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dependencies]
geometry_msgs = { version = "*", default-features = false }
libm = "0.2"

[features]
default = []
std = ["geometry_msgs/std"]
