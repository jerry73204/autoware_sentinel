# justfile for autoware_control_validator

default:
    @just --list

generate:
    cargo nano-ros generate --force

build:
    cargo build

test:
    cargo test

all: generate build

clean:
    rm -rf generated .cargo/config.toml target

clean-bindings:
    rm -rf generated .cargo/config.toml
