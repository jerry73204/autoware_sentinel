// Zenoh session config for rmw_zenoh_cpp clients.
// All ROS 2 nodes connect to the zenohd router at tcp/127.0.0.1:7447.
// Set via ZENOH_SESSION_CONFIG_URI in .envrc.
{
  mode: "client",
  connect: {
    // Use 127.0.0.1 (not localhost) to avoid IPv6 resolution issues under load.
    endpoints: ["tcp/127.0.0.1:7447"],
    timeout_ms: 30000,
  },
  scouting: {
    multicast: { enabled: false },
  },
  // Increase query timeout to 60s (default 10s). The first service call from
  // a new zenoh client can take 30-40s while zenoh-pico queryable routing is
  // established through the router.
  queries_default_timeout: 60000,
}
